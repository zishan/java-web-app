pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  environment {
    AWS_ACCESS_KEY='AKIAYITL6ZGALHUGXEHC'
	AWS_SECRET_KEY='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'
  }
  stages {
    stage('Deploy') {
      steps {
	    sh 'printenv'
	    sh 'docker context ls'
        sh 'docker context use myecscontext'
        sh 'docker compose up'
        sh 'docker compose ps --format json'
      }
    }
  }
}
